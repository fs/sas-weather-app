import { SearchDiv, SearchInput } from "./styles";

const SearchBar = () => {
  return (
    <SearchDiv>
      <SearchInput placeholder="Search" />
    </SearchDiv>
  );
};

export default SearchBar;
